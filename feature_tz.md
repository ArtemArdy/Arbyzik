# ТЗ: AI-хаб для разработчиков

## Feature-Driven Development · Канбан-формат

---

# ДОКУМЕНТ УПРАВЛЕНИЯ РАЗРАБОТКОЙ

**Версия:** 1.0
**Методология:** Feature Data-Driven Development (FDDD)
**Трекинг:** Канбан
**Стек:** FastAPI · OpenWebUI · MWS GPT · Redis · Qdrant · Whisper · PaddleOCR

---

# РАЗДЕЛ 1: PRODUCT VISION & METRICS

## 1.1 Проблема и решение

| Поле | Описание |
|------|----------|
| **Проблема** | Разработчики (Python, JS/TS, AI-инженеры) вынуждены переключаться между множеством AI-инструментов, вручную управлять контекстом и самостоятельно выбирать модели |
| **Решение** | Единый AI-хаб «Single UI Thread» — один интерфейс, автоматический выбор модели, персистентная память сессий |
| **Ключевая ценность** | Сложность скрыта под капотом. Пользователь разговаривает с одним ассистентом — система делает остальное |

## 1.2 Целевые метрики (Definition of Done на уровне продукта)

```
┌─────────────────────────────────────────────────────────────────┐
│  NORTH STAR METRICS                                             │
├──────────────────────────┬──────────────────────────────────────┤
│ Метрика                  │ Целевое значение                     │
├──────────────────────────┼──────────────────────────────────────┤
│ Latency маршрутизации    │ < 50ms (p95)                        │
│ Точность Intent Agent    │ > 92% на тестовом наборе            │
│ Memory retrieval recall  │ > 85% релевантных фактов            │
│ STT accuracy (Whisper)   │ > 95% WER на технических текстах    │
│ Uptime оркестратора      │ 99.5%                               │
│ Time-to-first-token      │ < 800ms (p50)                       │
└──────────────────────────┴──────────────────────────────────────┘
```

---


# РАЗДЕЛ 2: ЭПИКИ И ФИЧИ

## ЭПИК E-01: ИНФРАСТРУКТУРА И ОРКЕСТРАТОР

```
Приоритет: CRITICAL PATH
Зависимости: все остальные эпики зависят от E-01
```

---

### ФИЧА F-01-01: Базовый FastAPI оркестратор

**Канбан-карточка:**
```
┌─────────────────────────────────────────────────────────────────┐
│ F-01-01 · Базовый FastAPI оркестратор (Streaming Edition)       │
│ Эпик: E-01 · Приоритет: P0 · Размер: L                          │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как системный компонент, мне нужен событийно-ориентированный    │
│ диспетчер, который маршрутизирует запросы, инжектирует контекст │
│ и возвращает ответ потоком (SSE) для нативной интеграции с      │
│ OpenWebUI, выполняя операции с памятью в фоновом режиме.        │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ POST /v1/chat/completions мимикрирует под OpenAI API          │
│ ✓ Поддержка Server-Sent Events (SSE) при stream=true            │
│ ✓ Роутинг и инжекция контекста выполняются до генерации 1 токена│
│ ✓ Обновление Qdrant/Redis выполняется через BackgroundTasks     │
│ ✓ Кастомные метаданные (роутинг) передаются в status/citations  │
│ ✓ Graceful shutdown не обрывает in-flight потоки и фоновые таски│
├─────────────────────────────────────────────────────────────────┤
│ DATA CONTRACT (входные данные — стандарт OpenWebUI)             │
│ {                                                               │
│   "model": "mws-auto-router",                                   │
│   "messages": [                                                 │
│     {"role": "user", "content": "Текст или base64 картинки"}    │
│   ],                                                            │
│   "stream": true,                                               │
│   "user": "string (идентификатор пользователя)"                 │
│ }                                                               │
├─────────────────────────────────────────────────────────────────┤
│ DATA CONTRACT (выходные данные — Server-Sent Events)            │
│ event: message                                                  │
│ data: {"id": "...", "choices": [{"delta": {"content": "А"}}]}   │
│ ...                                                             │
│ event: message                                                  │
│ data: {"id": "...", "choices": [{"delta": {"content": "Б"}}]}   │
│ ...                                                             │
│ event: message                                                  │
│ data: [DONE]                                                    │
├─────────────────────────────────────────────────────────────────┤
│ МЕТРИКИ ГОТОВНОСТИ                                              │
│ □ TTFT (Time To First Token) включая роутинг < 300ms            │
│ □ Время блокировки Event Loop < 10ms на запрос                  │
│ □ 100% изоляция I/O памяти от основного потока ответа           │
└─────────────────────────────────────────────────────────────────┘
```
---

### ФИЧА F-01-02: Redis — кэш и реестр статусов моделей

**Канбан-карточка:**
```
┌─────────────────────────────────────────────────────────────────┐
│ F-01-02 · Redis: кэш сессий и реестр здоровья моделей          │
│ Эпик: E-01 · Приоритет: P0 · Размер: M                        │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как оркестратор, мне нужно хранить оперативный контекст        │
│ диалога (10 сообщений) и динамический реестр доступности LLM   │
│ для мгновенного принятия решения роутером.                     │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ Атомарный апдейт сессии: LPUSH + LTRIM (max 10) + INCR       │
│ ✓ Тип данных статуса: Redis Hash (позволяет менять latency)    │
│ ✓ Мониторинг: TTL статуса = 45s (при heartbeat 15s)            │
│ ✓ Реализован механизм «мягкого отсева» (degraded модели        │
│   получают штрафной коэффициент в математическом роутере)      │
│ ✓ Сериализация: JSON для сообщений, HashFields для метрик      │
├─────────────────────────────────────────────────────────────────┤
│ DATA SCHEMA                                                     │
│                                                                 │
│ session:{session_id}:context → List[                           │
│   {                                                             │
│     "role": "user|assistant",                                  │
│     "content": "string",                                       │
│     "modality": "text|voice|file",                             │
│     "meta": {"file_ids": [], "intent": "string"},              │
│     "tokens": number                                           │
│   }                                                             │
│ ]                                                               │
│                                                                 │
│ model:{model_id}:health (Hash) → {                             │
│   "status": "available|degraded|offline",                      │
│   "latency_p50": "ms",                                         │
│   "load_factor": "0.0-1.0",                                    │
│   "updated_at": "ISO8601"                                      │
│ }                                                               │
├─────────────────────────────────────────────────────────────────┤
│ МЕТРИКИ ГОТОВНОСТИ                                              │
│ □ Время принятия решения роутером на базе Redis < 5ms          │
│ □ Автоматическое переключение на fallback-модель при offline   │
│ □ Отсутствие утечек памяти (строгий TTL на все ключи)          │
└─────────────────────────────────────────────────────────────────┘
```
---

### ФИЧА F-01-03: Qdrant — инициализация векторной БД

**Канбан-карточка:**
```
┌─────────────────────────────────────────────────────────────────┐
│ F-01-03 · Qdrant: схема коллекций и базовые операции           │
│ Эпик: E-01 · Приоритет: P0 · Размер: M                        │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как система памяти, мне нужна векторная БД с чёткой схемой    │
│ для хранения среднесрочных и долгосрочных воспоминаний          │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ Создана коллекция midterm_memory                             │
│ ✓ Создана коллекция longterm_facts                             │
│ ✓ Индексы по полю session_id и user_id                         │
│ ✓ Реализованы операции: upsert, search(top_k), delete_by_id    │
│ ✓ Размерность векторов: 1536 совместимость с embedding-моделью│
├─────────────────────────────────────────────────────────────────┤
│ DATA SCHEMA                                                     │
│                                                                 │
│ Коллекция: midterm_memory                                       │
│ {                                                               │
│   "id": "uuid4",                                               │
│   "vector": [float x 1536],                                    │
│   "payload": {                                                  │
│     "session_id": "string",                                    │
│     "user_id": "string",                                       │
│     "type": "summary|raw_chunk",                               │
│     "parent_summary_id": "uuid4 | null",                       │
│     "content": "string",                                       │
│     "token_count": number,                                     │
│     "created_at": "ISO8601"                                    │
│   }                                                             │
│ }                                                               │
│                                                                 │
│ Коллекция: longterm_facts                                       │
│ {                                                               │
│   "id": "uuid4",                                               │
│   "vector": [float x 1536],                                    │
│   "payload": {                                                  │
│     "user_id": "string",                                       │
│     "session_id": "string | null",                             │
│     "scope": "session|global",                                 │
│     "fact_text": "string",                                     │
│     "fact_type": "rule|decision|constraint|preference",        │
│     "source_message_id": "uuid4",                              │
│     "confirmed_by_user": boolean,                              │
│     "created_at": "ISO8601",                                   │
│     "is_active": boolean                                       │
│   }                                                             │
│ }                                                               │
├─────────────────────────────────────────────────────────────────┤
│ МЕТРИКИ ГОТОВНОСТИ                                              │
│ □ Search latency < 20ms при 100k векторов (p95)                │
│ □ Upsert latency < 50ms                                        │
│ □ Интеграционные тесты: insert → search → verify               │
└─────────────────────────────────────────────────────────────────┘
```

---

```
┌─────────────────────────────────────────────────────────────────┐
│ F-01-04 · Агент суммаризации и долговременной памяти            │
│ Эпик: E-01 · Приоритет: P1 · Размер: L                          │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как система, мне нужен изолированный фоновый процесс, который   │
│ надежно переносит переполненный контекст из краткосрочной       │
│ памяти (Redis) в среднесрочную (Qdrant), сжимая смыслы без      │
│ влияния на latency пользовательских запросов.                   │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ Воркер подписан на очередь задач (Task Queue) в Redis (ARQ).  │
│ ✓ Триггер: получение события "session_limit_reached".           │
│ ✓ Транзакционность: сообщения удаляются из Redis (LTRIM) только │
│   после успешного подтверждения записи (ACK) от Qdrant.         │
│ ✓ Механизм Retry: 3 попытки при 5xx ошибках от LLM/Qdrant       │
│   с экспоненциальной задержкой (Exponential Backoff).           │
│ ✓ Rate Limiting: приоритет вызовов к LLM ниже, чем у API.       │
│ ✓ Изоляция: падение воркера не аффектит работу API-сервера.     │
├─────────────────────────────────────────────────────────────────┤
│ DATA CONTRACT (Payload задачи в очереди)                        │
│ {                                                               │
│   "task_id": "uuid4",                                           │
│   "session_id": "string",                                       │
│   "trigger": "token_limit | message_count | manual",            │
│   "raw_messages": [ ...массив из 10 сообщений... ]              │
│ }                                                               │
├─────────────────────────────────────────────────────────────────┤
│ МЕТРИКИ ГОТОВНОСТИ                                              │
│ □ Zero-Data-Loss: 0 потерянных сессий при kill -9 процесса.     │
│ □ Изоляция ресурсов: потребление CPU API-сервера не меняется.   │
│ □ Задержка появления факта в БД < 5 секунд после суммаризации.  │
└─────────────────────────────────────────────────────────────────┘
```

```
┌─────────────────────────────────────────────────────────────────┐
│ F-01-04.1 · Логика LLM-экстракции и дедупликации фактов         │
│ Эпик: E-01 · Приоритет: P1 · Размер: S (Суб-компонент)          │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как воркер памяти, мне нужен строгий системный промпт и         │
│ механизм проверки на дубли, чтобы извлекать из сырых диалогов   │
│ только атомарные факты и не засорять базу Qdrant повторами.     │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ API-вызов к LLM использует принудительный JSON-режим.         │
│ ✓ Промпт требует игнорировать small-talk и дробить решения.     │
│ ✓ Извлеченные факты атомарны (1 факт = 1 сущность/решение).     │
│ ✓ Перед upsert в longterm_facts (Qdrant) выполняется поиск      │
│   ближайших соседей. Если Cosine Similarity > 0.85, факт        │
│   обновляется (set_payload), а не дублируется.                  │
│ ✓ При ошибке парсинга JSON (ParseError) задача падает в ретрай. │
├─────────────────────────────────────────────────────────────────┤
│ DATA CONTRACT (Системный Промпт для легкой LLM)                 │
│ SYSTEM: "Ты аналитик архитектуры. Твоя цель - извлечь           │
│ инженерные факты и сжатый смысл из предоставленного диалога.    │
│ Выведи результат строго по JSON схеме:                          │
│ {                                                               │
│   "summary": "Краткая выжимка (до 200 символов)",               │
│   "atomic_facts": ["Факт 1", "Факт 2"]                          │
│ }"                                                              │
├─────────────────────────────────────────────────────────────────┤
│ МЕТРИКИ ГОТОВНОСТИ                                              │
│ □ JSON Parse Error Rate < 1% на 1000 тестовых прогонах.         │
│ □ Точность дедупликации (отсутствие смысловых клонов в БД).     │
│ □ LLM Latency на экстракцию < 2.5 секунд (используется Haiku).  │
└─────────────────────────────────────────────────────────────────┘

```



## ЭПИК E-02: НОРМАЛИЗАЦИЯ ВХОДЯЩИХ ДАННЫХ

```
Приоритет: P0
Зависимости: F-01-01
```

---

### ФИЧА F-02-01: Whisper STT — голосовой ввод

**Канбан-карточка:**
```
┌─────────────────────────────────────────────────────────────────┐
│ F-02-01 · Whisper STT: транскрипция аудио                      │
│ Эпик: E-02 · Приоритет: P0 · Размер: M                        │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как разработчик, я хочу говорить голосом вместо печати,        │
│ и получать точную транскрипцию технического текста             │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ Поддерживаемые форматы: WAV, MP3, OGG, WebM                 │
│ ✓ Максимальная длина аудио: 5 минут                            │
│ ✓ Язык определяется автоматически                              │
│ ✓ Модель: whisper-large-v3 (дефолт)                            │
│ ✓ Технические термины (названия библиотек, функций) сохраняются│
│ ✓ При ошибке транскрипции — возвращается код ошибки с деталями │
│ ✓ Транскрипция логируется для аудита                           │
├─────────────────────────────────────────────────────────────────┤
│ DATA CONTRACT (вход)                                            │
│ multipart/form-data:                                            │
│   audio: binary                                                 │
│   session_id: string                                            │
│   language: string | null (auto-detect если null)              │
│                                                                 │
│ DATA CONTRACT (выход)                                           │
│ {                                                               │
│   "transcript": "string",                                      │
│   "confidence": float (0.0-1.0),                               │
│   "language_detected": "string",                               │
│   "duration_sec": float,                                       │
│   "processing_time_ms": number                                 │
│ }                                                               │
├─────────────────────────────────────────────────────────────────┤
│ EDGE CASES                                                      │
│ □ Тишина / нечленораздельная речь → error: "no_speech"         │
│ □ Аудио > 5 мин → error: "duration_exceeded"                   │
│ □ Неподдерживаемый формат → error: "unsupported_format"        │
├─────────────────────────────────────────────────────────────────┤
│ МЕТРИКИ ГОТОВНОСТИ                                              │
│ □ WER < 5% на тестовом наборе технических текстов (RU/EN)      │
│ □ Latency транскрипции: < 3s для 60-секундного аудио           │
│ □ 20 параллельных запросов без деградации                      │
└─────────────────────────────────────────────────────────────────┘
```

---

### ФИЧА F-02-02: PaddleOCR — распознавание текста из изображений

**Канбан-карточка:**
```
┌─────────────────────────────────────────────────────────────────┐
│ F-02-02 · PaddleOCR: модальный препроцессор текста              │
│ Эпик: E-02 · Приоритет: P1 · Размер: M                          │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как система, мне нужно извлекать структурированный текст из     │
│ скриншотов кода и логов с сохранением отступов, чтобы экономить │
│ квоты дорогих мультимодальных LLM при чтении документации.      │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ Входной контракт мимикрирует под стандарт OpenAI Vision.      │
│ ✓ Декодирование base64 выполняется вне основного Event Loop.    │
│ ✓ Эвристика отступов: координаты X конвертируются в пробелы     │
│   для корректного парсинга отступов в Python/YAML.              │
│ ✓ Кэширование: MD5-хэш изображения проверяется в Redis (F-01-02)│
│   перед инференсом. При совпадении текст отдается из кэша.      │
│ ✓ Языки: RU, EN .                                    │
├─────────────────────────────────────────────────────────────────┤
│ DATA CONTRACT (Вход — внутренний прокси от Оркестратора)        │
│ {                                                               │
│   "model": "ocr-paddle",                                        │
│   "messages": [                                                 │
│     {                                                           │
│       "role": "user",                                           │
│       "content": [                                              │
│         {                                                       │
│           "type": "image_url",                                  │
│           "image_url": {"url": "data:image/png;base64,..."}     │
│         }                                                       │
│       ]                                                         │
│     }                                                           │
│   ]                                                             │
│ }                                                               │
├─────────────────────────────────────────────────────────────────┤
│ DATA CONTRACT (Выход)                                           │
│ {                                                               │
│   "extracted_text": "string (с сохраненными отступами)",        │
│   "is_cached_hit": boolean,                                     │
│   "warnings": ["string (например: низкий контраст)"]            │
│ }                                                               │
├─────────────────────────────────────────────────────────────────┤
│ БИЗНЕС-ПРАВИЛО (Интеллектуальный роутинг)                       │
│ 1. Intent Agent направляет картинку в OCR ТОЛЬКО если запрос    │
│    классифицирован как чтение логов/кода/документации.          │
│ 2. Для задач анализа UI, архитектурных схем и ER-диаграмм       │
│    изображение направляется в мультимодальную VLM (Vision-LLM). │
├─────────────────────────────────────────────────────────────────┤
│ МЕТРИКИ ГОТОВНОСТИ                                              │
│ □ Сохранение синтаксиса: код на Python запускается без          │
│   IndentationError в 90% случаев после распознавания.           │
│ □ Latency < 2s (без кэша) и < 10ms (с кэшем) для 1080p.         │
└─────────────────────────────────────────────────────────────────┘
```

---

## ЭПИК E-03: INTENT AGENT — АНАЛИЗ НАМЕРЕНИЙ

```
Приоритет: P0
Зависимости: F-01-01, F-01-02
```

---

### ФИЧА F-03-01: Intent Agent — классификатор запросов

**Канбан-карточка:**
```
┌─────────────────────────────────────────────────────────────────┐
│ F-03-01 · Intent Agent: классификация и пре-роутинг            │
│ Эпик: E-03 · Приоритет: P0 · Размер: L                        │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как оркестратор, мне нужен микро-мозг для мгновенного          │
│ определения намерений пользователя, чтобы не тратить дорогие   │
│ токены тяжелых LLM на простые или рутинные задачи.             │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ Модель: самая быстрая из пула MWS GPT (напр., Claude Haiku). │
│ ✓ API-вызов строго использует Structured Outputs (JSON Schema).│
│ ✓ Timeout: 2 секунды (жёсткий лимит).                          │
│ ✓ Fallback при таймауте: text_generation + accuracy.           │
│ ✓ Кэширование (Redis, TTL 30s): ключ учитывает модальность.    │
├─────────────────────────────────────────────────────────────────┤
│ ПРОМПТ-СХЕМА (Strict JSON Schema)                               │
│                                                                 │
│ {                                                               │
│   "task_type": "enum",                                         │
│   "intent": "string (< 20 слов)",                              │
│   "priority": "speed | accuracy",                              │
│   "context_switch": boolean,                                   │
│   "requires_memory": boolean,                                  │
│   "complexity": "low | medium | high",                         │
│   "confidence": float (0.0 - 1.0)                              │
│ }                                                               │
│                                                                 │
│ Task Type Enum:                                                 │
│   text_generation                                               │
│   code_generation                                               │
│   code_refactoring                                              │
│   code_review                                                   │
│   image_ocr           ← роутинг на F-02-02 (PaddleOCR)          │
│   visual_analysis     ← роутинг на VLM                          │
│   file_formatting                                               │
│   translation                                                   │
│   question_answering                                            │
│   summarization                                                 │
├─────────────────────────────────────────────────────────────────┤
│ КЭШИРОВАНИЕ (Дедупликация)                                      │
│ Redis Key:                                                      │
│ intent:{MD5(text + has_audio + has_image + last_message_id)}    │
├─────────────────────────────────────────────────────────────────┤
│ МЕТРИКИ ГОТОВНОСТИ                                              │
│ □ Accuracy > 95% на тестовом наборе (500+ запросов).           │
│ □ P95 Latency < 600ms (включая сетевой round-trip).            │
│ □ JSON Parse Error = 0% (гарантируется Structured Outputs).    │
└─────────────────────────────────────────────────────────────────┘
```

---

## ЭПИК E-04: МАТЕМАТИЧЕСКИЙ РОУТЕР

```
Приоритет: P0
Зависимости: F-03-01, F-01-02
```

---

### ФИЧА F-04-01: Матрица бенчмарков и детерминированная маршрутизация

**Канбан-карточка:**
```
┌─────────────────────────────────────────────────────────────────┐
│ F-04-01 · Адаптивный Энтропийный Роутер    │
│ Эпик: E-04 · Приоритет: P0 · Размер: L (усложнение из-за H)     │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как система, я должна анализировать энтропию (неуверенность)    │
│ первых токенов дешевой модели, чтобы мгновенно переключаться на │
│ мощную модель при превышении порога сложности.                  │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ Интеграция с LiteLLM Middleware для захвата `logprobs`.       │
│ ✓ Расчет энтропии Шеннона ($H$) по первым 5–10 токенам.         │
│ ✓ Динамический `intent.complexity`: если $H > 0.5$ → "high".    │
│ ✓ Early Exit: Прерывание (abort) генерации дешевой модели при   │
│   детектировании высокой сложности (экономия времени/токенов).  │
│ ✓ Graceful Degradation: Снижение порога $H$ при отказе провайдеров.│
│ ✓ Веса ($W$) калибруются под приоритет (Accuracy vs Latency).   │
├─────────────────────────────────────────────────────────────────┤
│ МАТЕМАТИЧЕСКОЕ ЯДРО (Оценка неуверенности)                      │
│                                                                 │
│ Расчет на каждый токен: $H = - \sum (p_i \cdot \log p_i)$       │
│ Итоговый Score: $Average(H_{1...10})$                           │
│ Порог (Threshold): 0.4 (Safe) | 0.6 (Creative)                  │
├─────────────────────────────────────────────────────────────────┤
│ АЛГОРИТМ (Adaptive Pipeline)                                    │
│                                                                 │
│ def smart_route(request, models_config):                        │
│   # 1. Первый проход (Draft Model - напр. Llama-Tiny/Ollama)    │
│   draft_resp = call_fast_model(request, logprobs=True, limit=10)│
│                                                                 │
│   # 2. Вычисление энтропии (Middleware Logic)                   │
│   avg_entropy = calculate_entropy(draft_resp.logprobs)          │
│                                                                 │
│   # 3. Фейс-контроль на основе энтропии                         │
│   if avg_entropy > 0.5 or request.force_high_complex:           │
│     intent.complexity = "high"                                  │
│     abort(draft_resp) # Останавливаем дешевую модель            │
│   else:                                                         │
│     return draft_resp # Продолжаем работу с дешевой             │
│                                                                 │
│   # 4. Ранжирование тяжелых моделей (если complexity == high)   │
│   candidates = [m for m in models_config if m.score >= 80]      │
│   for c in candidates:                                          │
│     c.final_score = (c.accuracy * 0.8) + (c.latency * 0.2)      │
│                                                                 │
│   return sorted(candidates, key=lambda x: x.final_score)[0]     │
├─────────────────────────────────────────────────────────────────┤
│ DATA CONTRACT (выход роутера)                                   │
│ {                                                               │
│   "selected_model": "gpt-5-pro",                                │
│   "entropy_detected": 0.68,                                     │
│   "routing_decision": "ELVATED_TO_HIGH_COMPLEXITY",             │
│   "is_degraded": false,                                         │
│   "processing_ms": 45                                           │
│ }                                                               │
├─────────────────────────────────────────────────────────────────┤
│ МЕТРИКИ ГОТОВНОСТИ                                              │
│ □ Анализ энтропии добавляет < 50мс к итоговому TTFT.            │
│ □ Корректное переключение на 90% "завальных" тестов HumanEval.  │
│ □ Отсутствие "костылей": логика встроена в CustomLogger LiteLLM.│
└─────────────────────────────────────────────────────────────────┘
```

---

## ЭПИК E-05: СИСТЕМА ПАМЯТИ (AGENTIC FLAT RAG)

```
Приоритет: P0
Зависимости: F-01-02, F-01-03
```

---

### ФИЧА F-05-01: Краткосрочная память (Redis Window)

**Канбан-карточка:**
```
┌─────────────────────────────────────────────────────────────────┐
│ F-05-01 · Краткосрочная память: Redis sliding window           │
│ Эпик: E-05 · Приоритет: P0 · Размер: S                        │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как оркестратор, мне нужна немедленная история диалога         │
│ (до 10 сообщений) для сборки контекстного промпта             │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ При каждом запросе добавляется запись в Redis List           │
│ ✓ Автоматический trim: list всегда ≤ 10 элементов             │
│   (LPUSH + LTRIM реализованы атомарно через Lua-скрипт)        │
│ ✓ Подсчёт токенов обновляется при каждой записи               │
│ ✓ При session_id = new — создаётся новый ключ автоматически    │
│ ✓ TTL сессии: 24 часа (обновляется при каждом обращении)      │
│ ✓ Retrieval: LRANGE session:{id}:messages → упорядоченный список  │
├─────────────────────────────────────────────────────────────────┤
│ ТРИГГЕРЫ ДЛЯ СРЕДНЕСРОЧНОЙ ПАМЯТИ                               │
│ Если token_count > THRESHOLD_TOKENS (конфиг: default 4000)     │
│ ИЛИ len(messages) >= 10 → эмит события: "summarize_trigger"    │
│ → запускает F-05-02                                             │
└─────────────────────────────────────────────────────────────────┘
```

---

### ФИЧА F-05-02: Среднесрочная память — суммаризация в Qdrant

**Канбан-карточка:**
```
┌─────────────────────────────────────────────────────────────────┐
│ F-05-02 · Среднесрочная память: суммаризация → Qdrant          │
│ Эпик: E-05 · Приоритет: P0 · Размер: L                        │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как система, при достижении лимитов памяти я должна сжать      │
│ диалог, сохранить смысл и очистить кэш без потери контекста    │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ Суммаризация асинхронная — пользователь НЕ ждёт              │
│ ✓ РАЗДЕЛЬНАЯ суммаризация: отдельно запросы, отдельно ответы   │
│ ✓ Сырые чанки сохраняются в Qdrant как дочерние узлы           │
│ ✓ Суммаризированный вектор — родительский узел                 │
│ ✓ После записи в Qdrant → Redis List очищается (LTRIM до 0)    │
│ ✓ token_count сбрасывается до 0                                │
│ ✓ Связь parent_summary_id обеспечивает иерархию               │
├─────────────────────────────────────────────────────────────────┤
│ PIPELINE СУММАРИЗАЦИИ                                           │
│                                                                 │
│ 1. Получить все сообщения из Redis                             │
│ 2. Разделить на requests[] и responses[]                        │
│ 3. Вызвать дешёвую LLM:                                        │
│    prompt_req: "Сожми эти запросы пользователя в 3-5 фактах"  │
│    prompt_res: "Сожми эти ответы ассистента в 3-5 ключевых    │
│                решениях"                                        │
│ 4. Получить embedding для каждого summary                      │
│ 5. Upsert в Qdrant (midterm_memory):                           │
│    - Родитель: summary vector + payload                        │
│    - Дети: raw chunk vectors с parent_summary_id               │
│ 6. Очистить Redis ключи сессии                                 │
│ 7. Логировать событие                                          │
├─────────────────────────────────────────────────────────────────┤
│ DATA SCHEMA (Qdrant payload)                                    │
│ Родительский узел (summary):                                   │
│ {                                                               │
│   "type": "summary",                                           │
│   "session_id": "...",                                         │
│   "user_id": "...",                                            │
│   "summary_type": "requests|responses",                        │
│   "content": "сжатый текст",                                   │
│   "source_message_count": number,                              │
│   "created_at": "ISO8601"                                      │
│ }                                                               │
│                                                                 │
│ Дочерний узел (raw_chunk):                                      │
│ {                                                               │
│   "type": "raw_chunk",                                         │
│   "parent_summary_id": "uuid4",                                │
│   "session_id": "...",                                         │
│   "content": "оригинальный текст сообщения",                   │
│   "role": "user|assistant",                                    │
│   "position": number                                           │
│ }                                                               │
├─────────────────────────────────────────────────────────────────┤
│ МЕТРИКИ ГОТОВНОСТИ                                              │
│ □ Суммаризация не блокирует ответ пользователю                 │
│ □ 0 потерь данных при суммаризации (верификация тест-кейсами)  │
│ □ Retrieval после суммаризации возвращает релевантный контекст │
│ □ Нагрузочный тест: 100 параллельных суммаризаций              │
└─────────────────────────────────────────────────────────────────┘
```

---

### ФИЧА F-05-03: Долгосрочная память — атомарные факты

**Канбан-карточка:**
```
┌─────────────────────────────────────────────────────────────────┐
│ F-05-03 · Долгосрочная память: извлечение и хранение фактов    │
│ Эпик: E-05 · Приоритет: P1 · Размер: L                        │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как система, я должна автоматически извлекать из диалога       │
│ ключевые решения, правила и ограничения и хранить их вечно     │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ Извлечение фактов — фоновый процесс (не блокирует ответ)     │
│ ✓ Используется дешёвая LLM с закрытым промптом                 │
│ ✓ Факты строго типизированы по схеме                           │
│ ✓ Дубликаты определяются через cosine similarity > 0.92        │
│   (дублирующий факт не создаётся, существующий обновляется)    │
│ ✓ Scope: session (привязан к сессии) или global (для юзера)    │
│ ✓ Факты с confirmed_by_user: false не инжектируются автоматически│
│   — помечаются как "pending confirmation"                       │
├─────────────────────────────────────────────────────────────────┤
│ ПРОМПТ ДЛЯ ИЗВЛЕЧЕНИЯ ФАКТОВ                                    │
│                                                                 │
│ Системный промпт:                                               │
│ "Проанализируй диалог. Найди атомарные факты категорий:        │
│  rule (правило), decision (решение), constraint (ограничение),  │
│  preference (предпочтение).                                     │
│  Верни ТОЛЬКО JSON массив. Если фактов нет — пустой массив."   │
│                                                                 │
│ OUTPUT:                                                         │
│ [                                                               │
│   {                                                             │
│     "fact_text": "Запрещено использовать библиотеку Log4j",    │
│     "fact_type": "rule",                                        │
│     "confidence": 0.97                                          │
│   }                                                             │
│ ]                                                               │
├─────────────────────────────────────────────────────────────────┤
│ PIPELINE                                                        │
│                                                                 │
│ 1. После получения ответа от LLM → async task                  │
│ 2. Отправить диалог (последние N сообщений) в LLM-экстрактор  │
│ 3. Получить массив фактов                                       │
│ 4. Для каждого факта:                                           │
│    a. Получить embedding                                        │
│    b. Search в longterm_facts (session + global) top_k=3       │
│    c. Если max_similarity > 0.92 → upsert (обновить)           │
│    d. Иначе → insert новый факт (confirmed_by_user: false)      │
│ 5. Уведомить фронтенд (SSE): "new_facts_pending"               │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│ МЕТРИКИ ГОТОВНОСТИ                                              │
│ □ Precision извлечения фактов > 90% (нет мусорных фактов)      │
│ □ Recall > 80% (ключевые факты не теряются)                    │
│ □ 0 дублирующихся фактов при 3 одинаковых упоминаниях          │
│ □ Фоновая задача не увеличивает latency ответа                 │
└─────────────────────────────────────────────────────────────────┘
```

---

### ФИЧА F-05-04: Memory Retrieval — инжекция контекста в промпт

**Канбан-карточка:**
```
┌─────────────────────────────────────────────────────────────────┐
│ F-05-04 · Memory Retrieval: сборка контекстного промпта        │
│ Эпик: E-05 · Приоритет: P0 · Размер: M                        │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как оркестратор, перед отправкой запроса в LLM я должен        │
│ собрать полный контекст из всех уровней памяти                 │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ Retrieval запускается параллельно для Redis и Qdrant         │
│ ✓ Из Redis: последние N сообщений (краткосрочная)              │
│ ✓ Из Qdrant midterm_memory: top_k=3 по cosine similarity       │
│ ✓ Из Qdrant longterm_facts: все активные факты сессии +        │
│   глобальные факты юзера (без векторного поиска, by filter)    │
│ ✓ Итоговый промпт соответствует схеме ниже                     │
│ ✓ Если суммарный контекст > context_window модели →            │
│   применяется стратегия truncation (приоритет: факты > summary │
│   > raw_history)                                               │
├─────────────────────────────────────────────────────────────────┤
│ СХЕМА ИТОГОВОГО ПРОМПТА                                         │
│                                                                 │
│ [SYSTEM]                                                        │
│ {base_system_prompt}                                            │
│                                                                 │
│ ## Долгосрочные факты проекта:                                  │
│ {longterm_facts_text}                                           │
│                                                                 │
│ ## Контекст из предыдущих сессий:                               │
│ {midterm_summary_text}                                          │
│                                                                 │
│ [MESSAGES]                                                      │
│ {short_term_history}  ← последние 10 из Redis                  │
│                                                                 │
│ [USER]                                                          │
│ {current_query}                                                 │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│ МЕТРИКИ ГОТОВНОСТИ                                              │
│ □ Total retrieval time < 30ms (p95)                            │
│ □ Тест: факт "запрещён Log4j" инжектируется при релевантном    │
│   запросе                                                       │
│ □ Тест: truncation не теряет факты при большом контексте       │
└─────────────────────────────────────────────────────────────────┘
```

---

## ЭПИК E-06: ИСПОЛНЕНИЕ ЗАПРОСОВ (EXECUTION)

```
Приоритет: P0
Зависимости: E-03, E-04, E-05
```

---

### ФИЧА F-06-01: LLM Execution — отправка запроса в целевую модель

**Канбан-карточка:**
```
┌─────────────────────────────────────────────────────────────────┐
│ F-06-01 · LLM Execution: вызов целевой модели MWS GPT          │
│ Эпик: E-06 · Приоритет: P0 · Размер: M                        │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как оркестратор, я должен отправить собранный промпт в         │
│ выбранную модель и получить ответ с поддержкой стриминга       │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ Поддержка streaming (SSE / chunked response)                 │
│ ✓ Timeout запроса: 60 сек (конфигурируемый)                    │
│ ✓ Retry policy: 3 попытки с exponential backoff (1s, 2s, 4s)  │
│ ✓ При ошибке модели → роутер выбирает следующую из Top-3       │
│ ✓ Логирование: model_used, tokens_in, tokens_out, latency      │
│ ✓ Обновление Redis: запись ответа в краткосрочную память        │
│ ✓ Обновление token_count в Redis                               │
├─────────────────────────────────────────────────────────────────┤
│ DATA CONTRACT (вход в MWS GPT API)                              │
│ {                                                               │
│   "model": "selected_model_id",                                │
│   "messages": [...],  ← собранный промпт                       │
│   "stream": true,                                               │
│   "max_tokens": number,                                         │
│   "temperature": float                                          │
│ }                                                               │
│                                                                 │
│ DATA CONTRACT (ответ пользователю)                              │
│ {                                                               │
│   "response_id": "uuid4",                                      │
│   "text": "string",                                            │
│   "model_used": "model_id",                                    │
│   "routing_explanation": "human-readable string",              │
│   "top3_models": [...],                                        │
│   "tokens_used": {"prompt": number, "completion": number},     │
│   "memory_injected": boolean,                                   │
│   "latency_ms": number                                         │
│ }                                                               │
├─────────────────────────────────────────────────────────────────┤
│ МЕТРИКИ ГОТОВНОСТИ                                              │
│ □ Time-to-first-token < 800ms (p50)                            │
│ □ Retry механизм протестирован на mock-падениях модели          │
│ □ Fallback на вторую модель работает прозрачно для пользователя│
└─────────────────────────────────────────────────────────────────┘
```

---

## ЭПИК E-07: OPENWEBUI — ФРОНТЕНД И ИНТЕГРАЦИЯ

```
Приоритет: P1
Зависимости: E-01, E-06
Примечание: интеграция строго по официальной документации OpenWebUI
```

---

### ФИЧА F-07-01: OpenWebUI — базовая интеграция с оркестратором

**Канбан-карточка:**
```
┌─────────────────────────────────────────────────────────────────┐
│ F-07-01 · OpenWebUI: подключение к FastAPI оркестратору        │
│ Эпик: E-07 · Приоритет: P1 · Размер: M                        │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как пользователь, я хочу работать в привычном интерфейсе       │
│ чата, который на самом деле общается с нашим оркестратором     │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ OpenWebUI подключён к FastAPI через OpenAI-compatible API    │
│ ✓ Все запросы проксируются через оркестратор                   │
│ ✓ Streaming ответов работает нативно                           │
│ ✓ Session_id синхронизируется с conversation_id OpenWebUI      │
│ ✓ Документация OpenWebUI соблюдена для всех точек интеграции   │
├─────────────────────────────────────────────────────────────────┤
│ ТЕХНИЧЕСКОЕ ПРИМЕЧАНИЕ                                          │
│ FastAPI оркестратор экспонирует OpenAI-compatible endpoint:     │
│ POST /v1/chat/completions                                       │
│ OpenWebUI настраивается на этот endpoint как кастомная модель  │
└─────────────────────────────────────────────────────────────────┘
```

---

### ФИЧА F-07-02: Виджет Top-3 моделей и объяснение выбора

**Канбан-карточка:**
```
┌─────────────────────────────────────────────────────────────────┐
│ F-07-02 · UI: виджет routing explanation + Top-3               │
│ Эпик: E-07 · Приоритет: P1 · Размер: M                        │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как пользователь, я хочу видеть, какая модель ответила мне     │
│ и почему, а также иметь возможность выбрать другую             │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ Под каждым ответом — плашка с именем использованной модели   │
│ ✓ Плашка раскрывается в виджет с Top-3 альтернативами          │
│ ✓ Для каждой модели: название + score + краткое объяснение     │
│ ✓ Кнопка "Использовать эту модель" — manual override           │
│ ✓ При manual override → запрос повторяется с выбранной моделью │
│ ✓ Виджет не отображается для PaddleOCR (OCR всегда детерминирован)│
├─────────────────────────────────────────────────────────────────┤
│ UI DATA CONTRACT (из backend)                                   │
│ {                                                               │
│   "model_used": "Gamma",                                       │
│   "routing_explanation":                                        │
│     "Выбрана для рефакторинга кода (точность: 0.93).           │
│      Приоритет — качество. Log4j-правило инжектировано.",      │
│   "top3_models": [                                              │
│     {"name": "Gamma", "score": 0.91, "reason": "лучший код"},  │
│     {"name": "Delta", "score": 0.84, "reason": "быстрее"},     │
│     {"name": "Alpha", "score": 0.79, "reason": "больший контекст"}│
│   ]                                                             │
│ }                                                               │
└─────────────────────────────────────────────────────────────────┘
```

---

### ФИЧА F-07-03: Голосовой ввод в интерфейсе

**Канбан-карточка:**
```
┌─────────────────────────────────────────────────────────────────┐
│ F-07-03 · UI: кнопка микрофона и визуализация записи           │
│ Эпик: E-07 · Приоритет: P1 · Размер: S                        │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как разработчик, я хочу нажать кнопку микрофона, сказать       │
│ запрос и автоматически отправить его без ручного ввода         │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ Кнопка микрофона в поле ввода (рядом с кнопкой отправки)    │
│ ✓ Визуализация записи: анимированная волна                     │
│ ✓ По окончании записи — автоматическая транскрипция            │
│ ✓ Транскрипт появляется в поле ввода (пользователь может       │
│   отредактировать перед отправкой)                              │
│ ✓ Индикатор: "Распознаю речь..." во время обработки Whisper    │
│ ✓ Fallback: если микрофон недоступен — кнопка задизеблена      │
│   с tooltip "Микрофон недоступен"                              │
├─────────────────────────────────────────────────────────────────┤
│ FLOW                                                            │
│ Нажать кнопку → запись → отпустить → POST /api/v1/query/voice  │
│ → получить transcript → вставить в поле → пользователь отправляет│
└─────────────────────────────────────────────────────────────────┘
```

---

### ФИЧА F-07-04: Интерфейс управления памятью (Memory Panel)

**Канбан-карточка:**
```
┌─────────────────────────────────────────────────────────────────┐
│ F-07-04 · UI: панель управления долгосрочными фактами          │
│ Эпик: E-07 · Приоритет: P2 · Размер: L                        │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как разработчик, я хочу видеть, что система "запомнила"        │
│ о моём проекте, редактировать эти факты и переводить           │
│ локальные факты в глобальные                                    │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ Боковая панель "Память" открывается кнопкой в шапке          │
│ ✓ Два таба: "Факты сессии" и "Глобальные факты"                │
│ ✓ Каждый факт отображается как карточка:                       │
│   - Текст факта                                                 │
│   - Тип (rule/decision/constraint/preference) — цветная метка  │
│   - Дата создания                                               │
│   - Статус: подтверждён / ожидает подтверждения               │
│ ✓ Действия над фактом:                                         │
│   - ✅ Подтвердить (confirmed_by_user → true)                  │
│   - ✏️ Редактировать текст                                     │
│   - 🌐 Перевести в глобальные (scope: session → global)        │
│   - 🗑️ Удалить (is_active → false)                            │
│ ✓ Уведомление при появлении новых фактов: badge на иконке      │
│ ✓ Поиск по фактам (клиентский)                                 │
├─────────────────────────────────────────────────────────────────┤
│ API ENDPOINTS для панели                                         │
│ GET  /api/v1/memory/facts?session_id=&scope=                   │
│ PATCH /api/v1/memory/facts/{fact_id}                           │
│ DELETE /api/v1/memory/facts/{fact_id}                          │
│ POST /api/v1/memory/facts/{fact_id}/promote (session→global)   │
└─────────────────────────────────────────────────────────────────┘
```

---

### ФИЧА F-07-05: Брендинг OpenWebUI под брендбук

**Канбан-карточка:**
```
┌─────────────────────────────────────────────────────────────────┐
│ F-07-05 · UI: адаптация дизайна OpenWebUI под брендбук         │
│ Эпик: E-07 · Приоритет: P2 · Размер: M                        │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как продукт, я должен выглядеть согласно брендбуку компании,   │
│ а не как стандартный OpenWebUI                                  │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ Цветовая палитра заменена на палитру из брендбука            │
│ ✓ Логотип и favicon заменены                                    │
│ ✓ Шрифты соответствуют брендбуку                               │
│ ✓ Название приложения изменено                                  │
│ ✓ Брендинг применяется через официальные механизмы OpenWebUI   │
│   (environment variables + custom CSS), НЕ через патчинг кода  │
├─────────────────────────────────────────────────────────────────┤
│ INPUT                                                           │
│ □ Брендбук (PDF/Figma) передаётся команде до начала фичи       │
│ □ Список переменных окружения OpenWebUI для кастомизации       │
└─────────────────────────────────────────────────────────────────┘
```

---

## ЭПИК E-08: МОНИТОРИНГ И OBSERVABILITY

```
Приоритет: P1
Зависимости: E-01
```

---

### ФИЧА F-08-01: Метрики и дашборд

**Канбан-карточка:**
```
┌─────────────────────────────────────────────────────────────────┐
│ F-08-01 · Observability: метрики, логи, трейсинг               │
│ Эпик: E-08 · Приоритет: P1 · Размер: M                        │
├─────────────────────────────────────────────────────────────────┤
│ USER STORY                                                      │
│ Как команда разработки, мы должны видеть состояние системы     │
│ и диагностировать проблемы без доступа к продакшн-серверу     │
├─────────────────────────────────────────────────────────────────┤
│ ACCEPTANCE CRITERIA                                             │
│ ✓ Метрики экспонируются в Prometheus-формате (/metrics)        │
│ ✓ Ключевые метрики:                                             │
│   - request_total (by task_type, model, status)                │
│   - request_latency_ms (histogram, p50/p95/p99)               │
│   - routing_latency_ms                                         │
│   - memory_retrieval_latency_ms                                │
│   - model_availability (gauge, by model_id)                    │
│   - summarization_trigger_total                                 │
│   - facts_extracted_total                                       │
│ ✓ Structured JSON логи для каждого запроса (request_id, trace) │
│ ✓ Алерты: latency > 3s → notify; model_availability < 2 → crit│
└─────────────────────────────────────────────────────────────────┘
```

---



---

# РАЗДЕЛ 5: ДЕРЕВО ЗАВИСИМОСТЕЙ

```
F-01-01 (FastAPI Core)
    │
    ├──► F-01-02 (Redis)
    │        │
    │        ├──► F-05-01 (Short-term memory)
    │        │        └──► F-05-02 (Mid-term memory) ─► F-05-04 (Retrieval)
    │        │
    │        └──► F-04-01 (Router) ◄─── F-03-01 (Intent Agent)
    │                                           ▲
    ├──► F-01-03 (Qdrant)                      │
    │        │                                  │
    │        ├──► F-05-02 (Mid-term)            │
    │        ├──► F-05-03 (Long-term facts)     │
    │        └──► F-05-04 (Retrieval)           │
    │                                            │
    ├──► F-02-01 (Whisper STT) ─────────────────┘
    ├──► F-02-02 (PaddleOCR) ──► F-04-01 (hardcoded rule)
    │
    └──► F-06-01 (LLM Execution)
             │
             └──► F-07-01 (OpenWebUI Integration)
                      ├──► F-07-02 (Top-3 Widget)
                      ├──► F-07-03 (Voice Input UI)
                      ├──► F-07-04 (Memory Panel)
                      └──► F-07-05 (Branding)

F-08-01 (Monitoring) ──► подключается к F-01-01 параллельно
```

---

# РАЗДЕЛ 6: ПРИОРИТИЗАЦИЯ И ПОРЯДОК ЗАПУСКА

## 6.1 Волны разработки

```
┌─────────────────────────────────────────────────────────────────┐
│ ВОЛНА 1: КРИТИЧЕСКИЙ ПУТЬ — ИНФРАСТРУКТУРА                      │
│ Цель: система принимает запросы и возвращает ответы             │
├─────────────────────────────────────────────────────────────────┤
│ F-01-01 → F-01-02 → F-01-03 (параллельно 01-02 и 01-03)       │
│ Результат: оркестратор + Redis + Qdrant готовы                  │
└─────────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────────┐
│ ВОЛНА 2: НОРМАЛИЗАЦИЯ И ИНТЕЛЛЕКТ                               │
│ Цель: система понимает что хочет пользователь                   │
├─────────────────────────────────────────────────────────────────┤
│ F-02-01 (Whisper) + F-02-02 (PaddleOCR) [параллельно]         │
│ F-03-01 (Intent Agent)                                          │
│ F-04-01 (Router)                                                │
│ Результат: маршрутизация работает end-to-end                    │
└─────────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────────┐
│ ВОЛНА 3: ПАМЯТЬ                                                 │
│ Цель: система помнит контекст между запросами                   │
├─────────────────────────────────────────────────────────────────┤
│ F-05-01 → F-05-02 → F-05-03 → F-05-04                         │
│ Результат: трёхуровневая память работает                        │
└─────────────────────────────────────────────────────────────────┘
         ↓
┌─────────────────────────────────────────────────────────────────┐
│ ВОЛНА 4: ИСПОЛНЕНИЕ И ФРОНТЕНД                                  │
│ Цель: пользователь работает через UI                            │
├─────────────────────────────────────────────────────────────────┤
│ F-06-01 (Execution)                                             │
│ F-07-01 → F-07-02 → F-07-03 [параллельно F-07-04, F-07-05]   │
│ F-08-01 (Monitoring) [параллельно]                              │
│ Результат: полная система в продакшне                           │
└─────────────────────────────────────────────────────────────────┘
```

---

# РАЗДЕЛ 7: DEFINITION OF DONE (ГЛОБАЛЬНЫЙ)

## Фича считается DONE если:

```
□ Все Acceptance Criteria выполнены и задокументированы
□ Unit-тесты написаны, coverage ≥ 80%
□ Интеграционные тесты прошли
□ Data contracts задокументированы в OpenAPI / схемах
□ Метрики готовности достигнуты (измерено, не на глаз)
□ Peer review пройден (минимум 1 апрувер)
□ Документация обновлена (README / Architecture Decision Records)
□ Мониторинг: метрика для фичи добавлена в F-08-01
□ Нет open P0/P1 багов
```

---

# РАЗДЕЛ 8: БИЗНЕС-ПРАВИЛА (ИНВАРИАНТНЫЕ)

```
┌─────────────────────────────────────────────────────────────────┐
│ ЖЁСТКИЕ ПРАВИЛА — НЕ ПОДЛЕЖАТ ИЗМЕНЕНИЮ БЕЗ ADR               │
├─────────────────────────────────────────────────────────────────┤
│ RULE-001: image_to_text задачи ВСЕГДА идут на PaddleOCR.       │
│           LLM для OCR не вызываются НИКОГДА.                   │
│           Проверяется в роутере жёстким условием (не весами).  │
│                                                                 │
│ RULE-002: Роутер выбирает модель БЕЗ вызова LLM.               │
│           Только математика + статичная матрица.               │
│                                                                 │
│ RULE-003: Суммаризация асинхронна.                             │
│           Пользователь НИКОГДА не ждёт суммаризацию.           │
│                                                                 │
│ RULE-004: Факты с confirmed_by_user: false НЕ инжектируются    │
│           автоматически в промпт. Только после подтверждения.  │
│                                                                 │
│ RULE-005: Intent Agent имеет timeout 2 секунды.                │
│           При превышении — fallback без исключения.             │
│                                                                 │
│ RULE-006: Интеграция с OpenWebUI только через официальные      │
│           механизмы (ENV + custom CSS). Не патчить исходники.  │
└─────────────────────────────────────────────────────────────────┘
```

---

# РАЗДЕЛ 9: РИСКИ И МИТИГАЦИИ

```
┌──────────────────────┬─────────────┬───────────────────────────┐
│ Риск                 │ Вероятность │ Митигация                 │
├──────────────────────┼─────────────┼───────────────────────────┤
│ MWS GPT API меняет  │ Средняя     │ Адаптер-слой между        │
│ интерфейс           │             │ роутером и API моделей    │
├──────────────────────┼─────────────┼───────────────────────────┤
│ Redis падает         │ Низкая      │ Graceful degradation:     │
│                      │             │ работа без кэша,          │
│                      │             │ только Qdrant             │
├──────────────────────┼─────────────┼───────────────────────────┤
│ Intent Agent         │ Средняя     │ Fallback-значения,        │
│ галлюцинирует enum  │             │ валидация ответа по схеме │
├──────────────────────┼─────────────┼───────────────────────────┤
│ Context window       │ Высокая     │ Truncation-стратегия      │
│ переполняется        │             │ (факты > summary > raw)   │
├──────────────────────┼─────────────┼───────────────────────────┤
│ OpenWebUI обновляет │ Средняя     │ Фиксация версии OpenWebUI │
│ API                  │             │ в docker-compose          │
└──────────────────────┴─────────────┴───────────────────────────┘
```

---

*ТЗ версия 1.0 — все изменения фиксируются через ADR (Architecture Decision Records)*
*Метрики пересматриваются после каждой волны разработки*
